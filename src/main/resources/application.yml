server:
  port: 8080
spring:
  datasource:
    driver-class-name: org.h2.Driver
    password: sa
    url: jdbc:h2:mem:insurance
    username: sa
  jpa:
    properties:
     hibernate:
      show_sql: true
    defer-datasource-initialization: true